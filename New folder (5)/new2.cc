#include<bits/stdc++.h>
using namespace std;
#include<math.h>
#define ll long long int
#define Pi 
int main(){
    int n;
    cin >> n;
    vector<ll> vec1, vec2;
    for (int i = 0; i < n; i++)
    {
        int x;
        cin >> x;
        vec1.push_back(x);
    }
}